1. As long as we are in the individual folder, only work on the UI. Do not connect anything in this individual folder to the backend.

2. Main thread is strictly reserved for UI widget rendering. Any non-UI function (such as data processing, sorting/filtering large lists, data calculations, or parsing) MUST be offloaded to a background Isolate using `Isolate.run()` or `compute()` to prevent blocking the UI thread and ensure 60/120 FPS performance. Only bypass Isolates for trivial O(1) synchronous UI state updates.

3. Make sure to add const modifiers to widgets that are not mutable.

4. Do not import any backend or repository code. You are only allowed to create UI widgets, forms, screens, and general-purpose Flutter components.

Make sure the code is clean and DRY

5. Reminder (Notification Module): When building the notification module later, connect the Intelligent Budget Analytics feature to trigger proactive push/in-app notifications suggesting budget tightening opportunities and goal completion acceleration.

6. Architectural File Structure: Any `.dart` file that contains a `Scaffold` widget must be dedicated strictly to that screen/page. Reusable widgets or widgets without a `Scaffold` must not share the same file with a `Scaffold` widget; they should be placed in their respective `widgets/` folder and separate file.